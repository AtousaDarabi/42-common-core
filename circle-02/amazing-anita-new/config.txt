# Mandatory keys
WIDTH=20
HEIGHT=10
ENTRY=1,1
EXIT=19,9
OUTPUT_FILE=maze.txt
PERFECT=True

# Optional keys
# SEED fixes the RNG so the same maze is reproduced every run.
SEED=42
# ALGORITHM: DFS, KRUSKAL, PRIM, or RANDOM (picks one of the three each run).
ALGORITHM=RANDOM
# DISPLAY: ASCII (default, terminal) or MLX (graphical window; run `make mlx`
# first to compile MiniLibX -- falls back to ASCII if that isn't available).
DISPLAY=MLX
# ANIMATE: True (the default) to watch the maze get carved out wall-by-wall
# on each regenerate ('r') instead of only seeing the finished maze; set to
# False to disable.
ANIMATE=True
# ANIMATE_DELAY: seconds paused between animation frames (default 0.03).
ANIMATE_DELAY=0.03
